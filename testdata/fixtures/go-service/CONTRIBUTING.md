# Contributing

Run the repository-owned verification target before requesting review:

```bash
make verify
```

Do not bypass the race-enabled test target for changes to storage or state transitions.
