Active artifact IDs:

- `use-protocol-packet-constants`
- `preserve-analytics-event-continuity`
- `synchronize-environment-configuration`
- `change-gateway-api-route`

Orientation implementation guidance: Preserve documented library, gateway, agent, and shared-protocol boundaries while changing behavior.

Routing summary: Classified as `implementation` affecting `gateway/api/**/*.go`. `add-feature-flag` is excluded because no feature-flagged rollout is requested. Verification recipes and automation proposals are inactive; no commands were run.